#include <windows.h>

// ReShade reads the add-on's version from here and shows it in its overlay, so
// a bug report can name the exact build instead of describing it.
VS_VERSION_INFO VERSIONINFO
 FILEVERSION    1,0,27,0
 PRODUCTVERSION 1,0,27,0
 FILEOS         VOS_NT_WINDOWS32
 FILETYPE       VFT_DLL
BEGIN
    BLOCK "StringFileInfo"
    BEGIN
        BLOCK "040904b0"
        BEGIN
            VALUE "FileDescription",  "DLSS 5 DX11 Bridge"
            VALUE "FileVersion",      "1.0.27.0"
            VALUE "InternalName",     "dlss5-dx11-bridge"
            VALUE "OriginalFilename", "dlss5-dx11-bridge.addon64"
            VALUE "ProductName",      "DLSS 5 DX11 Bridge"
            VALUE "ProductVersion",   "1.0.27.0"
        END
    END
    BLOCK "VarFileInfo"
    BEGIN
        VALUE "Translation", 0x409, 1200
    END
END
